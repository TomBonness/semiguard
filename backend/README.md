# Backend

Flask API that serves the trained PyTorch model for defect prediction.

## Setup

```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

Then run with `python app.py`, starts on http://localhost:5000.
